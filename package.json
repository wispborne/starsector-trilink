{
  "name": "trilink",
  "version": "1.0.0",
  "description": "TriLink — standalone client-side site that generates one-click \"Install with TriOS\" links for Starsector mod forum posts.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "gen-badges": "node scripts/gen-install-badges.js"
  },
  "license": "Apache-2.0"
}
